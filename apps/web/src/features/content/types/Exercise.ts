export type Exercise = {
    id: number;
    name: string;
    description: string;
    duration: number;
    createdAt: string;
    updatedAt: string;
    imageUrl?: string;
    audioUrl?: string;
};
